import React, { Component } from 'react';
import { render } from 'react-dom';

class FlashcardButton extends Component {
// const FlashcardButton = props => {
  render() {
    return (
      // <button onClick={this.props.flipCard}>{this.props.cardDisplay}</button>)
      // <button>TEST</button>)
      <button>{this.props.cardDisplay}</button>)
  }
}


export default FlashcardButton;
